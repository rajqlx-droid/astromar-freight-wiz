

User wants Compare/History buttons NOT to take extra vertical space when they wrap below the title on mid-width viewports (like current 920px). Currently the heading row stacks them on a new row below "Load Optimizer Calculator" because `flex-col lg:flex-row` only goes side-by-side at ≥1024px.

Let me check current state.

